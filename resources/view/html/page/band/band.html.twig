{% block title %} Band {% endblock %} {% extends "base.html.twig" %} {% block content %}
<div class="content container" id="container" page-id="band">
    <div class="row">
        <div class="col-lg-12">
            <div class="jumbotron">
                <div class="container">
                    <h2>Créer des groupes de douceurs</h2>
                </div>
            </div>
        </div>
    </div>
    {% for band in band.getAllBands() %}
    <div class="col-lg-3 col-sm-4 col-md-4 col-xs-4">
        <div class="thumbnail">
            <img src="{{ band.cover }}" alt="...">
            <div class="caption">
                <h3>{{ band.name }}</h3>
                <p>{{ band.agency }}</p>
                <p>{{ band.date_creation }}</p>
                <p>{{ band.description }}</p>
                <p>
                    <a href="/band/{{ band.name }}" class="btn btn-primary btn-info" role="button">Edit</a>
                </p>
            </div>
        </div>
      </div>
    {% endfor %} 
    </div>
    {% endblock %} 
    {% block javascript %}
    <script src="/assets/js/helper.js"></script>
    <script src="/assets/js/request.js"></script>
    <script src="/assets/js/band.js"></script>
    {% endblock %}